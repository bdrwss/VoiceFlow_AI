开发时由 `tauri.conf.json` 的 `beforeDevCommand: npm run dev` 启动 Vite 前端，Tauri 再加载 `http://localhost:1420`；首次运行需调用 `download_sensevoice` 从 ModelScope/HuggingFace/GitHub 镜像拉取 sherpa-onnx 引擎与 SenseVoice 模型，解压后落盘至应用数据目录下的 `sherpa-onnx/`。