Tauri v2 + tauri-plugin-opener / tauri-plugin-autostart；系统输入捕获用 `rdev`，键盘/剪贴板模拟用 `enigo` + `arboard`；网络下载与流式写入用 `reqwest` + `futures-util`；tar.bz2 解压用 `tar` + `bzip2`；SenseVoice 引擎为外部静态二进制 `sherpa-onnx-v1.10.33-win-x64-static`，通过 `std::process::Command` 子进程调用。