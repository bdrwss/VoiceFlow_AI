React + TypeScript + Vite 构建；UI 图标来自 lucide-react；音频采集使用浏览器原生 `getUserMedia` + `AudioWorklet`；ASR 支持 Xenova 本地 Whisper 与 SenseVoice Small（通过 `invoke` 调用 Rust 插件下载/推理），以及 Groq/OpenAI 兼容云端 API；LLM 润色默认对接 DeepSeek v1 Chat API；桌面壳为 Tauri v2（`@tauri-apps/api/core`、`plugin-fs`、`plugin-autostart`）。